import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sotlgotfol.app',
  appName: 'sotlgotfol',
  webDir: 'dist/sothe-last-guardian',
  server: {
    androidScheme: 'https'
  }
};

export default config;
